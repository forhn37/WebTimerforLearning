// import React from 'react';

const Timercontrolbutton: React.FC<{ title: string }> = ({ title }) => {
  return (
    <button type="button">{title}</button>
  );
};

export default Timercontrolbutton;
